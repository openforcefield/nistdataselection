name: test
channels:
  - conda-forge
  - omnia
  - openeye
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Main Requirements
  - pandas
  - openforcefield
  - smirnoff99frosst
  - matplotlib

    # Commercial
  - openeye-toolkits

    # Pip-only installs
  #- pip:
  #  - codecov

